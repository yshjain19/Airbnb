<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
  <div class="container-fluid px-3 px-md-4">

    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <i class="fa-solid fa-house"></i>
      <span>PrestigeStay</span>
    </a>

    <!-- Search Bar — Desktop center -->
    <form class="nav-search-form d-none d-lg-flex mx-auto" action="/listings" method="GET" role="search">
      <div class="nav-search-box">
        <input type="text" name="search" id="navSearchInput" class="nav-search-input"
          placeholder="Search destinations, stays..." autocomplete="off" />
        <button type="submit" class="nav-search-btn" aria-label="Search">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </div>
    </form>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">

      <!-- Search Bar — Mobile (inside collapsed menu) -->
      <form class="d-lg-none my-2 px-1" action="/listings" method="GET" role="search">
        <div class="nav-search-box w-100">
          <input type="text" name="search" id="navSearchInputMobile" class="nav-search-input"
            placeholder="Search destinations, stays..." autocomplete="off" />
          <button type="submit" class="nav-search-btn" aria-label="Search">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </div>
      </form>

      <div class="navbar-nav align-items-center gap-2">
        <a class="nav-link" href="/listings">All listings</a>
        <a class="nav-link" href="/listings/new">Create Listing</a>
        <% if (!currentUser) { %>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/signup">Signup</a>
          <% } else { %>
            <span class="nav-link text-muted">Hello, <%= currentUser.username %></span>
            <a class="nav-link" href="/logout">Logout</a>
            <% } %>
      </div>
    </div>
  </div>
</nav>